(ns clojuree.core)

;(def syms [:fn :> :< :+ :- :* :/ :let :if :first :rest :quote :a :b :c :d :e])

(defn foo
  "I don't do a whole lot."
  [x]
  (println x "Hello, World!"))

(defn -main []
  (print "ahoj")
)
